// Header con el logo de Vips para los emails transaccionales. Siempre el logo
// de marca (nunca la foto de un cliente) — es lo que representa a la empresa
// en cualquier email, no la identidad del destinatario.
export const LOGO_HTML = `<img src="${process.env.NEXT_PUBLIC_SITE_URL}/icon-512.png" alt="Vips" width="64" height="64" style="border-radius:12px;display:block;margin-bottom:16px;" />`;
